{
  "name": "startseite",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Startseite rosenbaum.hamburg",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
